<h1> All Recipes </h1>

<ul><% @recipes.each do |recipe| %>
  <li><%= link_to recipe.name, recipe %> </li>
  <% end %> </ul>

<%= link_to "New Recipe", new_recipe_path %> | <%= link_to "All Wines", wines_path %>  | <%= link_to "My Account", user_path(@user) %>
